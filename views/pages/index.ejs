<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>
</head>

<body>
	<img src="verne.jpg" />
	<ul>
    <% paths.forEach(function(r) { %>
        <li><a href="/path/<%= r.id %>"><%= r.name %></a></li>
    <% }); %>
</ul>
</body>
</html>